[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-smart-router"
version = "0.1.0"
authors = [
  { name="Kerem Barbaros Karnabat", email="kbarbaros@hotmail.com" }
]
description = "A lightweight CLI tool for delegating LLM tasks to expert models across multiple providers."
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "openai>=1.0.0",
    "filelock>=3.12.0",
    "anthropic>=0.30.0"
]

[project.scripts]
agent-smart-router = "agent_smart_router:main"

